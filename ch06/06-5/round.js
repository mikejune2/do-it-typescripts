let quotient = 1 / 3
let floor = Math.floor(quotient)   // 0
let round = Math.round(quotient)   // 0
let ceil = Math.ceil(quotient)     // 1

console.log("quotient=", quotient, "floor=", floor, "round=", round, "ceil=", ceil)
