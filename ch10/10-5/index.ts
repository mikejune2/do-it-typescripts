/*import Person from './Person'
console.log(new Person("Jane", 22), new Person("Sam", 33))

import {range, random} from './util'
console.log(
  range(100, 110).map(n => random(n))
)
*/
import {range, random} from './util2'
console.log( range(100, 110).map(n => random(n)) )
